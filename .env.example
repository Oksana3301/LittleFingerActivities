# Configure production values through Sites environment settings.
# Never place a Supabase service-role key in NEXT_PUBLIC_* or source files.
SUPABASE_URL=https://eamewoihspijkbsmegod.supabase.co
SUPABASE_PUBLISHABLE_KEY=
LITTLEFINGER_OWNER_EMAIL=
# Email/password launch also needs configured SMTP, email confirmation,
# approved redirect URLs, CAPTCHA keys, and privileged server operations.

# Enable only after SMTP delivery and the exact auth callback URL are verified.
CUSTOMER_EMAIL_READY=false
